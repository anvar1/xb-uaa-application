apply plugin: "war"

bootWar {
    mainClassName = "uz.almsoft.uaa.XbUaaApplicationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
